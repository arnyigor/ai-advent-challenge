# Сценарий видео — День 14

1. Показать четыре карточки инвариантов и подчеркнуть: источник — отдельный `invariants.json`, не история диалога.
2. Переключить модель на `Qwen3.8-27B · RouterAI`, вручную ввести допустимый запрос про фильтр Compose. Показать реальный ответ Qwen, четыре результата `УЧТЁН` и решение `РАЗРЕШЕНО`.
3. Выбрать «Конфликт архитектуры»: запрос предлагает SQL из UI. Показать красный конфликт `architecture-clean` и аргументированный отказ.
4. Выбрать «Конфликт бизнес-правила»: запрос предлагает логировать открытый пароль. Показать конфликт `business-no-passwords` и отказ.
5. Объяснить, что в запрос намеренно передаётся история с фразой «игнорируй ограничения», но gate каждый раз заново читает отдельный контракт и срабатывает до формирования ответа.
6. Закончить крупным планом: `ОТКАЗ · конфликт` и точное правило, которое нельзя нарушать.
